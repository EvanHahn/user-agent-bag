{
  "name": "user-agent-bag",
  "description": "RFC-respecting User-Agent serialization and deserialization",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=8.0.0"
  },
  "main": "./UserAgentBag.js",
  "files": [
    "README.md",
    "LICENSE.txt",
    "CHANGELOG.md",
    "parser.js",
    "UserAgentBag.js",
    "UserAgentBag.js.flow"
  ],
  "scripts": {
    "pretest": "npm run lint && npm run build && npm run test:flow",
    "build": "pegjs --output parser.js parser.pegjs",
    "lint": "standard --fix",
    "test": "ava",
    "test:flow": "flow status"
  },
  "dependencies": {},
  "devDependencies": {
    "ava": "^1.4.1",
    "flow-bin": "^0.98.1",
    "pegjs": "^0.10.0",
    "standard": "^12.0.1"
  }
}
